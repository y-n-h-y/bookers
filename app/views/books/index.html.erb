<div class="book-list">
    <h2>Books</h2>
    <div class="book-list_table">
        <table>
            <thead>
              <tr>
                 <th>title</th>
                 <th>body</th>
                 <th></th>
              </tr>
            </thead>
            <tbody>
              <% @books.each do |book| %>
                <tr>
                    <td><%= book.title %></td>
                    <td><%= book.body %></td>
                    <td><%= link_to 'Show', book_path(book.id), class:"list-link" %></td>
                    <td><%= link_to 'Edit', edit_book_path(book.id), class:"list-link" %></td>
                    <td><%= link_to 'Destroy', book_path(book.id), method: :delete, data: {confirm: "Are you sure？"}, class:"list-link" %></td>
                </tr>
                <% end %>
            </tbody>
        </table>
    </div>
</div>

<div class="new-book">

<h2>New&thinsp;Book</h2>

    <% if @book.errors.any? %>
    <%= @book.errors.count %>error sprohibited this book from being saved
    <% @book.errors.full_messages.each do |message| %>
    <%= message %>
    <% end %>
    <% end %>

<%= form_with model:@book, local:true do |f| %>

<h4>title</h4>
<%= f.text_field :title %>
<h4>body</h4>
<%= f.text_area :body %>

<div class="submit"><%= f.submit 'Create Book' %></div>
</div>

<% end %>


